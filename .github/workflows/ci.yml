name: CI

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: mgmacri/OP_Client-Companion-Governance/.github/workflows/ci.yml@ca441d52a40e54d1056f0b4a3a81e6fe903b7256
    with:
      node-version: "20"
      pnpm-version: "9"
      working-directory: "app/client-companion"

