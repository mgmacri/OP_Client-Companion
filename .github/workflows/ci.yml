name: CI

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: mgmacri/OP_Client-Companion-Governance/.github/workflows/ci.yml@3ab99a629a8cda1a1bf740bfb89827afc4047547
    with:
      node-version: "20"
      pnpm-version: "9"
      working-directory: "app/client-companion"

