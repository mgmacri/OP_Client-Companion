name: CI

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: mgmacri/OP_Client-Companion-Governance/.github/workflows/ci.yml@5d78f69377ae9de1e5d9d92df43026dfe4a555b6
    with:
      node-version: "20"
      pnpm-version: "9"
      working-directory: "app/client-companion"

